import models


for id in range(0, models.ilosc_karetek):
    krotka = models.get_karetka()
    #generuj karetke

for id in range(0, models.get_pracownik()):
    krotka = models.get_pracownik()
    #generuj pracownika

for id in range(0, models.get_dyzur()):
    krotka = models.get_dyzur()
    #generuj dyżur

for id in range(0, models.ilosc_pracownikow_na_dyzurze):
    krotka = models.get_pracownik_na_dyzurze()
    #generuj

for id in range(0, models.ilosc_wyposazen_w_karetce):
    krotka = models.get_wyposazenie_w_karetce()
    #generuj

for id in range(0, models.ilosc_szpitali):
    krotka = models.get_szpital()
    #generuj

for id in range(0, models.ilosc_oddzialow_szpitali):
    krotka = models.get_oddzialy_szpitalu()
    #generuj

for id in range(0, models.ilosc_zgloszen):
    krotka = models.get_zgloszenie()
    #generuj

for id in range(0, models.ilosc_poszkodowanych):
    krotka = models.get_poszkodowany()
    #generuj

for id in range(0, models.ilosc_schorzen_poszkodowanego):
    krotka = models.get_schorzenie_poszkodowanego()
    #generuj

for id in range(0, models.ilosc_typow_schorzen):
    krotka = models.get_typy_schorzenia()
    #generuj

for id in range(0, models.ilosc_schorzen):
    krotka = models.get_schorzenie()
    #generuj

for id in range(0, models.ilosc_typow_karetek_do_schorzenia):
    krotka = models.get_typ_karetki_do_schorzenia()
    #generuj

for id in range(0, models.ilosc_dyzurow_do_zgloszenia):
    krotka = models.get_dyzur_do_zgloszenia()
    #generuj

for id in range(0, models.ilosc_roli_pracownika):
    krotka = models.get_rola_pracownika()
    #generuj